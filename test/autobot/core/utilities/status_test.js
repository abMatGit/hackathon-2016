describe('status', function () {
    context('when checking status on single task', function () {
        it ('returns the full status + description of that task', function () {
        });
    });

    context('when checking status on user', function () {
        it ('returns the status of stories that user is currently working on', function () {
        });
    });

    context('when checking status on feature', function () {
        it ('returns the status and tree structure of all tasks relevant to that feature', function () {
        });
    });

    context('when checking status on universe', function () {
        it ('returns the status and tree structure of all tasks in universe', function () {
        });
    });
});
